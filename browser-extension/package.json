{
  "name": "applymate-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "ApplyMate browser extension — Greenhouse/Lever application form detection and field mapping (read-only MVP).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/scan.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^20",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "tsx": "^4.23.1",
    "typescript": "^5"
  }
}
